This folder contains all the source code.
  
- `ref_python` contains the reference Python code.

- `hw_core` contains the Verilog code for hardware acceleration modules.

- `xmss-reference` contains the modified XMSS software code with software optimizations and compatible with software-hardware co-design setup
